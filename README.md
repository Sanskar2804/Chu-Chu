
# 🚂 Chu-Chu: Train Schedule Viewer

**Chu-Chu** is a lightweight, frontend-only web application that displays Indian Railways train schedules. It parses raw CSV data to provide a searchable and paginated interface for viewing train timings, stations, and routes.

## 🌟 Features

* **Dynamic Data Loading:** Fetches and parses train data directly from a CSV file using vanilla JavaScript.
* **Real-time Search:** Filter results instantly by Train Name, Train Number, or Station Name.
* **Pagination:** Efficiently handles large datasets by splitting data into readable pages.
* **Clean UI:** A responsive and modern interface built with custom CSS.
* **Zero Dependencies:** No frameworks or libraries required—just pure HTML, CSS, and JS.

